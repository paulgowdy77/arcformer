import numpy as np
import random

## PUBLIC EVALUATION SET RULE 82

def generate_config():
    #colors = random.sample(range(0, 9), 2)

    config = {
        'fixed_colors': random.sample(range(0, 9), 2),
    }

    return config


def generate_example(config):
   

    return input, output



def example_func():
    return generate_example